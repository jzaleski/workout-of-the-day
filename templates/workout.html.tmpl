{{ define "workout" }}
<div class="workout-container" style="margin-bottom: 25px;">
  <div class="date">{{ .Date }}</div>
  <div class="goal">{{ .Goal }}</div>
  <div class="description">{{ .Description }}</div>
  {{ if .VotingEnabled }}
  <div class="completed-container">
    {{ if .Historical }}
    <div class="completed-box-disabled">
    {{ else }}
    <div class="completed-box">
    {{ end }}
      <form action="/workout/{{ .Date }}/completed" method="post">
      {{ if .Historical }}
        <input class="completed-submit-disabled" type="submit" disabled="disabled" value="Completed: {{ .Completed }}"/>
      {{ else if .MarkedCompleted }}
        <input class="completed-submit" type="submit" value="Completed: {{ .Completed }}"/>
      {{ else }}
        <input class="completed-submit" type="submit" value="I did it!"/>
      {{ end }}
      </form>
    </div>
  </div>
  {{ end }}
  {{ if .QuestionsEnabled }}
  <div class="questions-container">
    <label class="questions-label">Questions about this Workout?</label>
    {{ if .SmsTo }}
    <a class="sms-link" href="sms:{{ .SmsTo }}">{{ .SmsTo }}</a><span>
    {{ else if .MailTo }}
    <a class="mail-link" href="mailto:{{ .MailTo }}">{{ .MailTo }}</a><span>
    {{ end }}
  </div>
  {{ end }}
</div>
{{ end }}
